#include <euler.h>
#include "gtest/gtest.h"

TEST(Euler, OneIteration)
{
    ASSERT_NEAR(GetEulerNumber(1), e, Epsilon);
}
TEST(Euler, TwoIteration)
{
    ASSERT_NEAR(GetEulerNumber(2), e, Epsilon);
}
TEST(Euler, ThreeIteration)
{
    ASSERT_NEAR(GetEulerNumber(3), e, Epsilon);
}
TEST(Euler, FourIteration)
{
    ASSERT_NEAR(GetEulerNumber(4), e, Epsilon);
}
TEST(Euler, FiveIteration)
{
    ASSERT_NEAR(GetEulerNumber(5), e, Epsilon);
}
TEST(Euler, SixIteration)
{
    ASSERT_NEAR(GetEulerNumber(6), e, Epsilon);
}
TEST(Euler, SevenIteration)
{
    ASSERT_NEAR(GetEulerNumber(7), e, Epsilon);
}
TEST(Euler, EightIteration)
{
    ASSERT_NEAR(GetEulerNumber(8), e, Epsilon);
}
TEST(Euler, NineIteration)
{
    ASSERT_NEAR(GetEulerNumber(9), e, Epsilon);
}
TEST(Euler, TenIteration)
{
    ASSERT_NEAR(GetEulerNumber(10), e, Epsilon);
}
TEST(Euler, ElevenIteration)
{
    ASSERT_NEAR(GetEulerNumber(11), e, Epsilon);
}
TEST(Euler, TwelveIteration)
{
    ASSERT_NEAR(GetEulerNumber(12), e, Epsilon);
}
TEST(Euler, ThirteenIteration)
{
    ASSERT_NEAR(GetEulerNumber(14), e, Epsilon);
}
TEST(Euler, FourteenIteration)
{
    ASSERT_NEAR(GetEulerNumber(15), e, Epsilon);
}
TEST(Euler, FifteenIteration)
{
    ASSERT_NEAR(GetEulerNumber(16), e, Epsilon);
}
TEST(Euler, SixteenIteration)
{
    ASSERT_NEAR(GetEulerNumber(17), e, Epsilon);
}
TEST(Euler, SeventeenIteration)
{
    ASSERT_NEAR(GetEulerNumber(18), e, Epsilon);
}